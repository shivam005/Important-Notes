## Nice process (Process which can give their share of CPU to other processes)
It has priority between -20 to 19, we can change the priority using nice and renice command. Higher niceness like 19 means lower priority and lower niceness like
-20 means higher priority. 

### There are two types of apps like IO bound (Heavy Network operation) and CPU bound (Heavy CPU Utilization)
